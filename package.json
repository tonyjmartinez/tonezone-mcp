{
  "name": "tonezone-mcp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create tonezone-store",
    "db:migrate": "wrangler d1 execute tonezone-store --file=schema.sql",
    "db:migrate:local": "wrangler d1 execute tonezone-store --local --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.5.2",
    "wrangler": "^4.0.0"
  }
}
